---
description: Regras de neg�cio e especifica��o � integra��o GitHub/Jira e pipeline de commits
alwaysApply: true
---

# Integra��o GitHub + Jira (m�dulo da plataforma)

## Contexto

- Este repo **� a Engineering Platform** � ver regra `engineering-platform.mdc`.
- Husky, commitlint, GitHub Actions etc. s�o **capabilities reutiliz�veis** para provisionar qualquer projeto da empresa.
- PDFs em `infra/docs/specs/` = spec do chefe. **PDF prevalece** em caso de d?vida.

## Fonte da verdade

1. `infra/docs/specs/integracao-github-jira-validacao-de-commits.pdf`
2. `infra/docs/specs/pnpm-commit-script-interativo-lint-typecheck.pdf`

## Conven��es obrigat�rias

- **Jira key**: `^[A-Z]{2,10}-[0-9]+` (ex.: `INT-001`)
- **Branch**: `tipo/CHAVE-descricao-curta`
- **Commit**: `tipo(escopo): CHAVE descricao` via `pnpm commit`
- **Smart Commits** (opcional): `MBX-123 #comment ... #time 1h #close`

## Valida��o em 3 camadas

1. **Local**: Husky (pre-commit ? lint-staged; commit-msg ? commitlint + jira key)
2. **CI**: GitHub Actions no PR contra `main`
3. **Branch protection**: check obrigat�rio antes do merge

## Princ�pios

- Implementar pensando em **qualquer projeto cliente**, n�o s� este repo.
- Mensagens de erro claras: qual commit falhou e formato esperado.
- Exce��es documentadas para dependabot, merges autom�ticos, hotfix (`--no-verify` s� em emerg�ncia).
